# Test Vectors

These deterministic vectors define the v0.1 wire format. Production code must
use random keys and nonces; fixed values are only for cross-implementation tests.

Each vector includes fixed keys, origin, ids, plaintext payloads, nonces, derived
keys, and expected encrypted envelopes.
